# Assignment 2 – CNN on CIFAR-10

## How to Run

Clone the repository:

git clone https://github.com/Baddala-Govardhan/CSCI611_Govardhan_Baddala.git

Install required dependencies:

pip install torch torchvision matplotlib numpy

Run the program:

python pytorch.py


## Output

The script will:

- Train the CNN model
- Print training and validation loss
- Print final test accuracy
- Save all required graphs and visualizations inside the `output/` folder


## Screenshots

The `output/` folder contains execution screenshots demonstrating
